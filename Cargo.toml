[package]
name = "sele"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "A native desktop harness orchestrator"

[dependencies]
adw = { package = "libadwaita", version = "0.9.2", features = ["v1_7"] }
gtk = { package = "gtk4", version = "0.11.4", features = ["v4_16"] }
