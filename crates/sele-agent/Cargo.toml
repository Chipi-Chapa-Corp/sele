[package]
name = "sele-agent"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "ACP runtime and agent integrations for Sele"

[dependencies]
agent-client-protocol = "=1.3.0"
async-channel = "2.5.0"
sele-core = { path = "../sele-core" }
tokio = { version = "1.47.1", features = ["rt-multi-thread", "time"] }
