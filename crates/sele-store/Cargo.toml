[package]
name = "sele-store"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Persistent read models for Sele"

[dependencies]
directories = "6.0.0"
rusqlite = { version = "0.40.2", features = ["bundled"] }
sele-core = { path = "../sele-core" }

[dev-dependencies]
tempfile = "3.27.0"
