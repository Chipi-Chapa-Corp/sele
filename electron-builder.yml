appId: com.chipichapa.sele
productName: Sele
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - node_modules/@github/copilot*/**
  - node_modules/@lydell/node-pty*/**
win:
  executableName: sele
  icon: build/icon.ico
  target:
    - nsis
nsis:
  artifactName: ${name}-windows-${arch}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  icon: build/icon.icns
  target:
    - dmg
  category: public.app-category.developer-tools
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
dmg:
  artifactName: ${name}-macos-${arch}.${ext}
linux:
  icon: build/icons
  target:
    - flatpak
  category: Development
  synopsis: Desktop AI harness for coding agents
  syncDesktopName: true
flatpak:
  artifactName: ${name}-linux-x64.${ext}
  runtime: org.freedesktop.Platform
  runtimeVersion: '25.08'
  sdk: org.freedesktop.Sdk
  base: org.electronjs.Electron2.BaseApp
  baseVersion: '25.08'
  branch: stable
  useWaylandFlags: true
  files:
    - - build/com.chipichapa.sele.metainfo.xml
      - /share/metainfo/com.chipichapa.sele.metainfo.xml
    - - LICENSE
      - /share/licenses/com.chipichapa.sele/LICENSE
  finishArgs:
    - --socket=wayland
    - --socket=fallback-x11
    - --share=ipc
    - --device=dri
    - --share=network
    - --talk-name=org.freedesktop.Flatpak
npmRebuild: false
